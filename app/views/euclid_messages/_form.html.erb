<%= form_for(@euclid_message) do |f| %>
  <% if @euclid_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@euclid_message.errors.count, "error") %> prohibited this euclid_message from being saved:</h2>

      <ul>
      <% @euclid_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, rows: 6%>
	<%= f.label :"Use %link% for a callback" %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
